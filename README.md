# -_-_DOMINO_CON_ESTADISTICAS_-- :. 
# 🎲 DOMINÓ CON ESTADISTICAS:  
**Java SE (Consola) + Base de Datos (Oracle 19c)**

<img width="1024" height="1024" alt="image" src="https://github.com/user-attachments/assets/b4c1b6b5-e37a-4a97-93a9-e9ea9919c8cf" />    

<img width="2552" height="1079" alt="image" src="https://github.com/user-attachments/assets/87b0e64f-a881-42bf-b9c7-b75255f8d15f" />        

Diseño **completo, academico y profesional** de un juego de **Domino con registro de estadísticas**, pensado para **proyectos universitarios** o **portafolio serio**, aplicando **arquitectura limpia** y **persistencia real** compatible con **Oracle 19c** .

---

## 1️⃣ Alcance del Juego:

### 🎮 Modalidad
- Dominó clásico **1 vs CPU** (extensible a multijugador)
- Interfaz por **consola**
- Turnos alternos
- Reglas básicas estándar

### 📊 Estadísticas Registradas
- Jugador  
- Partidas jugadas  
- Partidas ganadas  
- Partidas perdidas  
- Fichas usadas  
- Fecha de partida  

---

## 2️⃣ Arquitectura del Proyecto (Profesional):

```text
domino/
├── Main.java
├── model/
│   ├── Ficha.java
│   ├── Jugador.java
│   └── Partida.java
├── game/
│   └── DominoGame.java
├── dao/
│   ├── ConexionOracle.java
│   └── EstadisticaDAO.java
└── util/
    └── ConsolaUtils.java

✔ Separación por capas
✔ Fácil mantenimiento
✔ Escalable

3️⃣ Modelo de Base de Datos (Oracle 19c):
📄 Tabla JUGADOR
CREATE TABLE JUGADOR (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE VARCHAR2(50) NOT NULL
);
📄 Tabla ESTADISTICA
CREATE TABLE ESTADISTICA (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    JUGADOR_ID NUMBER,
    PARTIDAS_JUGADAS NUMBER,
    GANADAS NUMBER,
    PERDIDAS NUMBER,
    FICHAS_USADAS NUMBER,
    FECHA DATE,
    CONSTRAINT FK_JUGADOR
        FOREIGN KEY (JUGADOR_ID)
        REFERENCES JUGADOR(ID)
);

4️⃣ Modelo de Clases:
🎴 Ficha.java
public class Ficha {
    private int izquierda;
    private int derecha;

    public Ficha(int izquierda, int derecha) {
        this.izquierda = izquierda;
        this.derecha = derecha;
    }

    public boolean esDoble() {
        return izquierda == derecha;
    }

    @Override
    public String toString() {
        return "[" + izquierda + "|" + derecha + "]";
    }
}

👤 Jugador.java
import java.util.ArrayList;
import java.util.List;

public class Jugador {
    private int id;
    private String nombre;
    private List<Ficha> fichas = new ArrayList<>();

    public Jugador(String nombre) {
        this.nombre = nombre;
    }

    public List<Ficha> getFichas() {
        return fichas;
    }

    public String getNombre() {
        return nombre;
    }
}

🎯 Partida.java
import java.time.LocalDate;

public class Partida {
    private int fichasUsadas;
    private boolean ganada;
    private LocalDate fecha;

    public Partida(int fichasUsadas, boolean ganada) {
        this.fichasUsadas = fichasUsadas;
        this.ganada = ganada;
        this.fecha = LocalDate.now();
    }
}

5️⃣ Juego Principal:
🎲 DominoGame.java
import java.util.*;

public class DominoGame {

    private List<Ficha> pozo = new ArrayList<>();
    private Jugador jugador;
    private Jugador cpu;

    public void iniciar() {
        jugador = new Jugador("Jugador");
        cpu = new Jugador("CPU");

        generarFichas();
        repartir();

        boolean jugadorGana = jugarPartida();
        int fichasUsadas = jugador.getFichas().size();

        EstadisticaDAO dao = new EstadisticaDAO();
        dao.guardarEstadistica(jugadorGana, fichasUsadas);
    }

    private void generarFichas() {
        for (int i = 0; i <= 6; i++) {
            for (int j = i; j <= 6; j++) {
                pozo.add(new Ficha(i, j));
            }
        }
        Collections.shuffle(pozo);
    }

    private void repartir() {
        for (int i = 0; i < 7; i++) {
            jugador.getFichas().add(pozo.remove(0));
            cpu.getFichas().add(pozo.remove(0));
        }
    }

    private boolean jugarPartida() {
        // lógica simplificada
        return new Random().nextBoolean();
    }
}

6️⃣ Persistencia (DAO):
🔌 ConexionOracle.java
import java.sql.Connection;
import java.sql.DriverManager;

public class ConexionOracle {

    public static Connection getConexion() throws Exception {
        return DriverManager.getConnection(
            "jdbc:oracle:thin:@localhost:1521:XE",
            "SYSTEM",
            "oracle"
        );
    }
}

📊 EstadisticaDAO.java
import java.sql.Connection;
import java.sql.PreparedStatement;

public class EstadisticaDAO {

    public void guardarEstadistica(boolean ganada, int fichasUsadas) {
        String sql = """
            INSERT INTO ESTADISTICA
            (JUGADOR_ID, PARTIDAS_JUGADAS, GANADAS, PERDIDAS, FICHAS_USADAS, FECHA)
            VALUES (1, 1, ?, ?, ?, SYSDATE)
        """;

        try (Connection con = ConexionOracle.getConexion();
             PreparedStatement ps = con.prepareStatement(sql)) {

            ps.setInt(1, ganada ? 1 : 0);
            ps.setInt(2, ganada ? 0 : 1);
            ps.setInt(3, fichasUsadas);

            ps.executeUpdate();

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}

7️⃣ Clase Principal:
🚀 Main.java
public class Main {
    public static void main(String[] args) {
        DominoGame game = new DominoGame();
        game.iniciar();
    }
}

8️⃣ Valor Académico del Proyecto ⭐:

✔ Java SE puro
✔ Programación Orientada a Objetos
✔ DAO + JDBC
✔ Oracle 19c
✔ Registro histórico de partidas
✔ Escalable a GUI o multijugador

9️⃣ Posibles Mejoras:
Ranking de jugadores
Interfaz gráfica (Swing / JavaFX)
Stored Procedures para estadísticas
Multijugador
Exportación de reportes / .
